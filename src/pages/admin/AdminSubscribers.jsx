import {useEffect,useState} from "react";
export default function AdminSubscribers(){const [rows,setRows]=useState([]);const [error,setError]=useState("");useEffect(()=>{fetch("/api/admin/subscribers",{headers:{Authorization:`Bearer ${sessionStorage.getItem("naystrip_admin_session")}`}}).then(async(response)=>{const data=await response.json();if(!response.ok)throw new Error(data.error);setRows(data.subscribers||[])}).catch((err)=>setError(err.message))},[]);return <div><h1 className="font-display text-2xl">Newsletter Subscribers</h1><p className="mt-1 text-sm text-navy-500">Stored website newsletter signups.</p>{error&&<p className="mt-4 rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<div className="mt-6 overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[640px] text-sm"><thead><tr className="border-b text-left"><th className="p-4">Email</th><th className="p-4">Source</th><th className="p-4">Status</th><th className="p-4">Subscribed</th></tr></thead><tbody>{rows.map((row)=><tr key={row.id} className="border-b"><td className="p-4">{row.email}</td><td className="p-4 capitalize">{row.source}</td><td className="p-4 capitalize">{row.status}</td><td className="p-4">{new Date(row.created_at).toLocaleString("en-IN")}</td></tr>)}{!rows.length&&!error&&<tr><td colSpan="4" className="p-8 text-center text-slate-400">No subscribers yet.</td></tr>}</tbody></table></div></div>}

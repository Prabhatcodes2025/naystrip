import {Link,useLocation,useParams} from "react-router-dom";import {CheckCircle2,Clock3} from "lucide-react";import BrandLogo from "../components/branding/BrandLogo";
export default function BookingConfirmation(){const {reference}=useParams();const payment=useLocation().state?.payment;const pending=payment?.confirmation==="webhook_pending";return <main className="grid min-h-screen place-items-center bg-[#fffaf2] p-5"><section className="max-w-xl bg-white p-8 text-center shadow-lg sm:p-12"><BrandLogo className="mx-auto h-28"/>{pending?<Clock3 className="mx-auto mt-5 text-orange-500" size={48}/>:<CheckCircle2 className="mx-auto mt-5 text-emerald-600" size={48}/>}<h1 className="mt-5 font-display text-4xl text-[#173c34]">{pending?"Payment received":"Booking confirmed"}</h1><p className="mt-3 text-slate-600">{pending?"The secure Razorpay webhook is completing final verification. Your dashboard will update automatically.":"Your NaysTrip journey is ready in your dashboard."}</p><p className="mt-5 font-mono font-bold">{reference}</p><Link to="/account/dashboard" className="btn-primary mt-7">Open customer dashboard</Link></section></main>}

import { useState } from "react";
import { Link } from "react-router-dom";
import { MessageCircle, Phone, Plus, X } from "lucide-react";
import { getSiteSettings, whatsappHref } from "../../data/siteConfig";
export default function FloatingActions(){const [open,setOpen]=useState(false);const settings=getSiteSettings();return <div className="fixed bottom-[max(18px,env(safe-area-inset-bottom))] right-4 z-40 flex flex-col items-end gap-2 sm:right-6">{open&&<div className="grid gap-2 rounded-sm bg-white p-2 shadow-2xl"><a href={whatsappHref("Hi NaysTrip, I would like help planning a trip.")} target="_blank" rel="noopener noreferrer" className="flex min-h-11 items-center gap-3 px-4 text-sm font-bold text-[#173c34]"><MessageCircle size={18}/> WhatsApp</a><a href={`tel:${settings.phone.replace(/\D/g,"")}`} className="flex min-h-11 items-center gap-3 px-4 text-sm font-bold text-[#173c34]"><Phone size={18}/> Call</a><Link to="/custom-trip" className="min-h-11 bg-orange-500 px-4 py-3 text-sm font-bold text-white">Get a free quote</Link></div>}<button onClick={()=>setOpen(!open)} aria-expanded={open} aria-label={open?"Close contact options":"Open contact options"} className="grid h-14 w-14 place-items-center rounded-full bg-orange-500 text-white shadow-xl hover:bg-orange-600">{open?<X/>:<Plus/>}</button></div>}

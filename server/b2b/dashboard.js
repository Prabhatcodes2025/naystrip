import {json,supabaseRequest} from "../_shared.js";import {requirePortalUser} from "../_auth.js";
export default async function handler(req,res){const session=await requirePortalUser(req,res,"agent");if(!session)return;if(req.method!=="GET")return json(res,405,{error:"Method not allowed"});const ratesResponse=await supabaseRequest("package_agent_rates?active=eq.true&select=id,retail_price,agent_price,markup,commission,valid_from,valid_until,package:packages(id,slug,title,destination_names,days,nights,status)&order=created_at.desc");const bookingsResponse=await supabaseRequest(`bookings?agent_id=eq.${session.profile.id}&select=id,reference,travel_date,traveller_count,total,amount_paid,balance_due,payment_state,operational_status,package:packages(title),documents:booking_documents(id,document_type,created_at)&order=created_at.desc&limit=100`);return json(res,200,{agent:session.profile,rates:ratesResponse.ok?await ratesResponse.json():[],bookings:bookingsResponse.ok?await bookingsResponse.json():[]})}

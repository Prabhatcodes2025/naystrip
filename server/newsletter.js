import {guard,json,supabaseRequest} from "./_shared.js";
import {rateLimit} from "./_rate-limit.js";
import {normalizeEmail,validEmail} from "./_validation.js";
export default async function handler(req,res){if(!guard(req,res))return;if(!rateLimit(req))return json(res,429,{error:"Too many requests. Please try again shortly."});const email=normalizeEmail(req.body?.email);if(!validEmail(email))return json(res,422,{error:"Enter a valid email address"});try{const response=await supabaseRequest("newsletter_subscribers?on_conflict=email",{method:"POST",headers:{Prefer:"resolution=ignore-duplicates,return=minimal"},body:JSON.stringify({email,source:"homepage",status:"subscribed"})});if(!response.ok){console.error("newsletter_signup_failed",{status:response.status,statusText:response.statusText});return json(res,502,{error:"Subscription could not be saved. Please try again."})}return json(res,200,{subscribed:true})}catch(error){console.error("newsletter_signup_failed",{code:error?.code||"unavailable"});return json(res,500,{error:"Newsletter service is temporarily unavailable"})}}

import {json,supabaseRequest} from "./_shared.js";
export async function requireAdmin(req,res){const token=String(req.headers.authorization||"").replace(/^Bearer\s+/i,"");if(!token){json(res,401,{error:"Admin sign-in required"});return null}const url=process.env.SUPABASE_URL;const anon=process.env.SUPABASE_ANON_KEY;if(!url||!anon){json(res,503,{error:"Admin service is not configured"});return null}const auth=await fetch(`${url}/auth/v1/user`,{headers:{apikey:anon,Authorization:`Bearer ${token}`}});if(!auth.ok){json(res,401,{error:"Session expired"});return null}const user=await auth.json();const check=await supabaseRequest(`admin_users?user_id=eq.${encodeURIComponent(user.id)}&status=eq.active&select=user_id,display_name,role_id`);const rows=await check.json();if(!check.ok||!rows[0]){json(res,403,{error:"Admin access required"});return null}return {...rows[0],email:user.email}}

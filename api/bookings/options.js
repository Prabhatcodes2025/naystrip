import {json,supabaseRequest} from "../_shared.js";
export default async function handler(req,res){if(req.method!=="GET")return json(res,405,{error:"Method not allowed"});const slug=String(req.query?.slug||"").replace(/[^a-z0-9-]/gi,"").slice(0,120);if(!slug)return json(res,422,{error:"Package is required"});try{const response=await supabaseRequest(`packages?slug=eq.${encodeURIComponent(slug)}&status=eq.published&deleted_at=is.null&select=id,slug,title,destination_names,nights,days,overview,price_from,tax_percent,advance_percent,booking_enabled,custom_enquiry_only,start_point,end_point,package_departures(id,start_date,end_date,capacity,booked_seats,available_seats,price_override,advance_amount,booking_cutoff,status,meeting_point),package_addons(id,name,description,unit_amount,pricing_unit)&limit=1`);const [pkg]=await response.json();if(!response.ok||!pkg)return json(res,404,{error:"Package not found"});pkg.package_departures=(pkg.package_departures||[]).filter((departure)=>["open","filling_fast"].includes(departure.status)&&(!departure.booking_cutoff||new Date(departure.booking_cutoff)>new Date()));return json(res,200,{package:pkg})}catch(error){if(error.message==="SERVICE_NOT_CONFIGURED")return json(res,503,{error:"Live booking availability is being configured"});return json(res,500,{error:"Could not load booking options"})}}

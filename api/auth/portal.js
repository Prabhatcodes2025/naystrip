import {guard,json} from "../_shared.js";
export default async function handler(req,res){if(!guard(req,res))return;const {email,password,portal}=req.body||{};if(!email||!password||!['customer','agent'].includes(portal))return json(res,422,{error:"Complete all required fields"});if(!process.env.SUPABASE_URL||!process.env.SUPABASE_ANON_KEY)return json(res,503,{error:"Portal authentication is being configured"});const auth=await fetch(`${process.env.SUPABASE_URL}/auth/v1/token?grant_type=password`,{method:"POST",headers:{apikey:process.env.SUPABASE_ANON_KEY,"Content-Type":"application/json"},body:JSON.stringify({email,password})});const data=await auth.json();if(!auth.ok)return json(res,401,{error:"Email or password is incorrect"});return json(res,200,{access_token:data.access_token,expires_in:data.expires_in});}

import {guard,json} from "../_shared.js";
export default async function handler(req,res){if(!guard(req,res))return;const token=String(req.headers.authorization||"").replace(/^Bearer\s+/i,"");const password=String(req.body?.password||"");if(!token||password.length<10)return json(res,422,{error:"A valid recovery session and 10-character password are required"});if(!process.env.SUPABASE_URL||!process.env.SUPABASE_ANON_KEY)return json(res,503,{error:"Password update is not configured"});const response=await fetch(`${process.env.SUPABASE_URL}/auth/v1/user`,{method:"PUT",headers:{apikey:process.env.SUPABASE_ANON_KEY,Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({password})});return response.ok?json(res,200,{updated:true}):json(res,400,{error:"The recovery link is invalid or expired"})}

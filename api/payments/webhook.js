import crypto from "node:crypto";import {json,supabaseRequest} from "../_shared.js";import {bookingReference,money} from "../_validation.js";import {deliverNotification} from "../_notifications.js";import {generateAndStoreBookingDocuments} from "../_document-service.js";
export const config={api:{bodyParser:false}};const safe=(a,b)=>{const x=Buffer.from(a||"");const y=Buffer.from(b||"");return x.length===y.length&&crypto.timingSafeEqual(x,y)};async function readRaw(req){const chunks=[];for await(const chunk of req)chunks.push(Buffer.from(chunk));return Buffer.concat(chunks)}
export default async function handler(req,res){if(req.method!=="POST")return json(res,405,{error:"Method not allowed"});if(!process.env.RAZORPAY_WEBHOOK_SECRET)return json(res,503,{error:"Webhook not configured"});const raw=await readRaw(req);const expected=crypto.createHmac("sha256",process.env.RAZORPAY_WEBHOOK_SECRET).update(raw).digest("hex");if(!safe(expected,req.headers["x-razorpay-signature"]))return json(res,400,{error:"Invalid webhook signature"});let event;try{event=JSON.parse(raw.toString("utf8"))}catch{return json(res,400,{error:"Invalid payload"})}const eventId=String(event.id||req.headers["x-razorpay-event-id"]||crypto.createHash("sha256").update(raw).digest("hex"));const inserted=await supabaseRequest("payment_webhook_events",{method:"POST",headers:{Prefer:"resolution=ignore-duplicates,return=representation"},body:JSON.stringify({id:eventId,provider:"razorpay",event_type:event.event||"unknown",payload:event,processing_status:"received"})});const rows=inserted.ok?await inserted.json():[];if(!rows.length)return json(res,200,{received:true,duplicate:true});try{const entity=event?.payload?.payment?.entity||event?.payload?.refund?.entity;const orderId=entity?.order_id||event?.payload?.order?.entity?.id;if(!orderId){await finish(eventId,"ignored");return json(res,200,{received:true,ignored:true})}const lookup=await supabaseRequest(`payments?gateway_order_id=eq.${encodeURIComponent(orderId)}&select=*,booking:bookings(id,reference,total,advance_required,amount_paid,balance_due,customer_id,travel_date,billing,ticket_number,ticket_generated_at,package:packages(title))&limit=1`);const [payment]=await lookup.json();if(!payment)throw new Error("Payment record not found");const status=["payment.captured","order.paid"].includes(event.event)?"successful":event.event==="payment.failed"?"failed":event.event==="refund.processed"?"refunded":event.event==="refund.failed"?"failed":"pending";await supabaseRequest(`payments?id=eq.${payment.id}`,{method:"PATCH",body:JSON.stringify({gateway_payment_id:entity?.id||payment.gateway_payment_id,gateway_transaction_id:entity?.id||payment.gateway_transaction_id,status,payment_method:entity?.method||payment.payment_method,captured_at:status==="successful"?new Date().toISOString():payment.captured_at,raw_status:{event:event.event,status:entity?.status},metadata:{fee:entity?.fee,tax:entity?.tax},updated_at:new Date().toISOString()})});if(status==="successful")await confirmPayment(payment,eventId,entity);else if(status==="failed")await supabaseRequest(`bookings?id=eq.${payment.booking_id}`,{method:"PATCH",body:JSON.stringify({payment_state:"failed",operational_status:"pending_payment",updated_at:new Date().toISOString()})});else if(status==="refunded")await supabaseRequest(`bookings?id=eq.${payment.booking_id}`,{method:"PATCH",body:JSON.stringify({payment_state:"refunded",operational_status:"refunded",status:"cancelled",updated_at:new Date().toISOString()})});await finish(eventId,"processed");return json(res,200,{received:true})}catch(error){await finish(eventId,"failed",error.message);console.error("razorpay_webhook_failed",error);return json(res,500,{error:"Webhook processing failed"})}}
async function confirmPayment(payment,eventId,entity){const paidResponse=await supabaseRequest(`payments?booking_id=eq.${payment.booking_id}&status=eq.successful&select=amount`);const paidRows=await paidResponse.json();const amountPaid=money(paidRows.reduce((sum,row)=>sum+Number(row.amount),0));const balance=money(Math.max(0,Number(payment.booking.total)-amountPaid));const fullyPaid=balance===0;const confirmed=amountPaid>=Number(payment.booking.advance_required);const ticket=payment.booking.ticket_number||bookingReference("NTT");await supabaseRequest(`bookings?id=eq.${payment.booking_id}`,{method:"PATCH",body:JSON.stringify({amount_paid:amountPaid,balance_due:balance,payment_state:fullyPaid?"fully_paid":"partially_paid",operational_status:confirmed?(fullyPaid?"fully_paid":"confirmed"):"pending_payment",status:confirmed?"confirmed":"pending",ticket_number:ticket,ticket_generated_at:payment.booking.ticket_generated_at||new Date().toISOString(),razorpay_payment_id:entity?.id||null,updated_at:new Date().toISOString()})});await supabaseRequest("booking_activity",{method:"POST",body:JSON.stringify({booking_id:payment.booking_id,action:fullyPaid?"payment_completed":"payment_received",actor_type:"webhook",details:{event_id:eventId,amount:payment.amount,balance}})});if(confirmed)try{await generateAndStoreBookingDocuments(payment.booking_id)}catch(error){console.error("post_payment_documents_failed",error.message)}const email=payment.booking.billing?.email;const phone=payment.booking.billing?.phone;const payload={reference:payment.booking.reference,customerName:payment.booking.billing?.name,packageTitle:payment.booking.package?.title,travelDate:payment.booking.travel_date,amountPaid,balanceDue:balance,portalUrl:`${process.env.PUBLIC_SITE_URL||"https://naystrip.vercel.app"}/account/dashboard`};await Promise.all([email&&deliverNotification({bookingId:payment.booking_id,event:confirmed?"booking_confirmed":"payment_received",recipient:email,payload,idempotencyKey:`${payment.booking.reference}:${confirmed?"booking_confirmed":"payment_received"}:email`}),phone&&deliverNotification({bookingId:payment.booking_id,event:confirmed?"booking_confirmed":"payment_received",recipient:phone,channel:"whatsapp",payload,idempotencyKey:`${payment.booking.reference}:${confirmed?"booking_confirmed":"payment_received"}:whatsapp`})].filter(Boolean))}
async function finish(id,status,error=null){await supabaseRequest(`payment_webhook_events?id=eq.${encodeURIComponent(id)}`,{method:"PATCH",body:JSON.stringify({processing_status:status,error,processed_at:new Date().toISOString()})})}

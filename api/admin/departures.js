import {requireAdmin} from "../_admin.js";import {json,supabaseRequest} from "../_shared.js";import {clean,dateOnly,money,uuidPattern} from "../_validation.js";
export default async function handler(req,res){const admin=await requireAdmin(req,res);if(!admin)return;const body=req.body||{};if(req.method==="GET"){const response=await supabaseRequest("package_departures?select=*,package:packages(title,slug)&order=start_date.asc");return response.ok?json(res,200,{departures:await response.json()}):json(res,502,{error:"Departures could not be loaded"})}if(!["POST","PATCH","DELETE"].includes(req.method))return json(res,405,{error:"Method not allowed"});if(req.method==="DELETE"){if(!uuidPattern.test(req.query?.id))return json(res,422,{error:"Invalid departure"});await supabaseRequest(`package_departures?id=eq.${req.query.id}`,{method:"PATCH",body:JSON.stringify({status:"cancelled"})});return json(res,200,{cancelled:true})}if(!uuidPattern.test(body.packageId)||!dateOnly(body.startDate)||!dateOnly(body.endDate)||Number(body.capacity)<1)return json(res,422,{error:"Package, dates and capacity are required"});const record={package_id:body.packageId,start_date:body.startDate,end_date:body.endDate,capacity:Number(body.capacity),booked_seats:Math.max(0,Number(body.bookedSeats||0)),price_override:body.price===""?null:money(body.price),advance_amount:body.advanceAmount===""?null:money(body.advanceAmount),booking_cutoff:body.bookingCutoff||null,status:["open","filling_fast","sold_out","closed","completed","cancelled"].includes(body.status)?body.status:"open",meeting_point:clean(body.meetingPoint,500),internal_notes:clean(body.internalNotes,2000)};const response=body.id?await supabaseRequest(`package_departures?id=eq.${body.id}`,{method:"PATCH",headers:{Prefer:"return=representation"},body:JSON.stringify(record)}):await supabaseRequest("package_departures",{method:"POST",headers:{Prefer:"return=representation"},body:JSON.stringify(record)});return response.ok?json(res,body.id?200:201,{departure:(await response.json())[0]}):json(res,502,{error:"Departure could not be saved"})}

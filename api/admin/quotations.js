import {requireAdmin} from "../_admin.js";import {json,supabaseRequest} from "../_shared.js";
const clean=(value,max=2000)=>String(value??"").trim().replace(/[<>]/g,"").slice(0,max);const money=(value)=>Math.max(0,Math.round(Number(value||0)*100)/100);
export default async function handler(req,res){const admin=await requireAdmin(req,res);if(!admin)return;try{
 if(req.method==="GET"){const response=await supabaseRequest("quotations?select=*&order=created_at.desc&limit=200");const rows=await response.json();return response.ok?json(res,200,{quotations:rows}):json(res,502,{error:"Unable to load quotations"})}
 if(req.method!=="POST")return json(res,405,{error:"Method not allowed"});const body=req.body||{};const lines=(body.lines||[]).slice(0,100).map((line,index)=>({description:clean(line.description,500),quantity:Math.max(.01,Number(line.quantity||1)),unit_price:money(line.unitPrice),sort_order:index})).filter((line)=>line.description);if(!clean(body.customerName,120)||!clean(body.title,200)||!lines.length)return json(res,422,{error:"Customer, title and at least one line item are required"});
 const subtotal=money(lines.reduce((sum,line)=>sum+line.quantity*line.unit_price,0));const discount=Math.min(subtotal,money(body.discount));const taxable=Math.max(0,subtotal-discount);const tax=money(taxable*(Math.min(100,Math.max(0,Number(body.taxPercent||0)))/100));const reference=`NTQ-${new Date().toISOString().slice(0,10).replaceAll("-","")}-${crypto.randomUUID().slice(0,6).toUpperCase()}`;
 const quote={reference,inquiry_id:clean(body.inquiryId,60)||null,customer_name:clean(body.customerName,120),customer_email:clean(body.customerEmail,160)||null,customer_phone:clean(body.customerPhone,24)||null,title:clean(body.title,200),valid_until:body.validUntil||null,subtotal,discount,tax,total:money(taxable+tax),terms:clean(body.terms,5000)||null,notes:clean(body.notes,5000)||null,created_by:admin.user_id};const insert=await supabaseRequest("quotations",{method:"POST",headers:{Prefer:"return=representation"},body:JSON.stringify(quote)});if(!insert.ok)return json(res,502,{error:"Unable to create quotation"});const [created]=await insert.json();const lineInsert=await supabaseRequest("quotation_lines",{method:"POST",body:JSON.stringify(lines.map((line)=>({...line,quotation_id:created.id})))});if(!lineInsert.ok)return json(res,502,{error:"Quotation created but line items could not be saved"});return json(res,201,{quotation:created});
 }catch(error){console.error("quotations_failed",error);return json(res,500,{error:"Quotation service unavailable"})}}
